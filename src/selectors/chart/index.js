const currentIndicatorSelector = state => state.chartReducer.currentIndicator;
const currentPeriodSelector = state => state.chartReducer.currentPeriod;

export { currentIndicatorSelector, currentPeriodSelector };
